{
  "name": "@delgadotrueba/http",
  "version": "0.0.1",
  "description": "httpService and httpMockService",
  "author": "delgadotrueba",
  "keywords": [
    "http"
  ],
  "scripts": {
    "build": "npx tsc -p ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "main": "index.js",
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "tslib": "^2.6.3"
  }
}
